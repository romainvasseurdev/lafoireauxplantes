<!DOCTYPE html>

    <head>
        <meta charset="UTF-8">
        <title>{% block title %}La Foire aux Plantes{% endblock %}</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
        {% block stylesheets %}{% endblock %}
    </head>

    <body style="min-height: 900px; background-image: url('https://images.wallpaperscraft.com/image/leaves_plant_green_136320_1920x1080.jpg'); background-repeat: no-repeat; background-size:cover;background-position: center center;">
            <nav class="navbar navbar-expand-lg navbar-light bg-dark">
                    <a class="navbar-brand" href="{{ path('home') }}" style="color: white;">Accueil</a>
                    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                      <span class="navbar-toggler-icon"></span>
                    </button>
                  
                    <div class="collapse navbar-collapse" id="navbarSupportedContent">
                      <ul class="navbar-nav mr-auto">
                        <li class="nav-item active">
                          <a class="nav-link" href="{{ path('allposts.show') }}" style="color: white;">Les annonces</a>
                        </li>
                        {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item dropdown">
                          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" style="color: white;">
                            Modération
                          </a>
                          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="{{ path('admin.show') }}">Annonces</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ path('admin.users') }}">Utilisateurs</a>
                          </div>
                        </li>
                        {% endif %}
                        {% if not app.user %}
                        <li class="nav-item active">
                          <a class="nav-link" href="{{ path('security.registration') }}" style="color: white;">S'inscrire</a>
                        </li>
                      </ul>
                      <ul class="navbar-nav">
                        <li class="nav-item active float-left">
                          <a class="nav-link btn btn-success" href="{{ path('security.login') }}" style="color: white">Se connecter</a>
                          </li>
                        {% else %}
                        <li class="nav-item dropdown">
                          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" style="color: white;">
                            Mon profil
                          </a>
                          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="{{ path('user.myprofile', {id: app.user.id}) }}">Voir mon profil</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ path('user.edit.profile', {id: app.user.id}) }}">Éditer mon profil</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ path('user.show') }}">Gérer mes annonces</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ path('user.wishlist') }}">Gérer ma wishlist</a>
                          </div>
                        </li>
                      </ul>
                        <ul class="navbar-nav">
                          <li class="navbar-text white" style="color: white;">
                            {% if app.user %}
                            Bonjour {{ app.user.username }}, j'espère que tu vas bien aujourd'hui !&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                            {% endif %}
                            </li>
                          <li class="nav-item active">
                            <a class="nav-link btn btn-success" href="{{ path('security.logout') }}" style="color: white; float: left;">Se déconnecter</a>
                          </li>
                        {% endif %}
                      </ul>
                    </div>
                  </nav>
                    {% block body %}{% endblock %}
                  
        
        
        {% block javascripts %}{% endblock %}
        <script src="https://code.jquery.com/jquery-3.4.1.slim.min.js" integrity="sha384-J6qa4849blE2+poT4WnyKhv5vZF5SrPo0iEjwBvKU7imGFAV0wwj1yYfoRSJoZ+n" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>
      </div>

  <footer class="bg-dark fixed-bottom" style="background-color: grey;width: 100%;">
    <div class="container">
      <div class="footer-copyright" style="color: white;">© Copyright 2020 La foire aux plantes | <a target="blank" href="{{ path('mentions') }}">Mentions legales</a> | <a target="blank" href="{{ path('cgu') }}">CGU</a> | <a target="blank" href="{{ path('cgv') }}">CGV</a> | <a href="mailto:lafoireauxplantes.contact@gmail.com?subject=La foire aux plantes">Me contacter</a></div>
    </div>  
    </footer>

</body>

</html>
